# Registros CRM — Infraestructura

## Vercel (recomendado)

Frontend + API en un solo proyecto. Ver **`VERCEL.md`** en la raíz del repo.

## Contratos API

Definidos en `apps/web/src/lib/mock/handlers.ts` (referencia) e implementados en `apps/api`.
